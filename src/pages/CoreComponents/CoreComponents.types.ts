export type CoreComponentsProps = {
  // Define page props here if needed
};

export const variants = [
  "primary",
  "secondary",
  "ghost",
  "danger",
  "success",
  "warning",
  "info",
  "link",
] as const;
